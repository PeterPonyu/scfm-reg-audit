# Reference environment for the scReg-Eval code capsule.
# Python 3.13.7, x86-64 Linux. The audit and its tests are CPU-only.
# These are the versions the public reference outputs were produced under.
anndata==0.12.3
h5py==3.15.1
numpy==2.2.6
pandas==2.3.3
pyfaidx==0.9.0.4
scikit-learn==1.6.1
scipy==1.16.2

# Optional: needed only to rerun foundation-model readouts and the motif
# scan on your own data. `python -m unittest` and validate_artifacts.py
# run without them.
# MOODS-python==1.9.4.1
# einops==0.8.2
# torch==2.12.1
# transformers==5.12.1
